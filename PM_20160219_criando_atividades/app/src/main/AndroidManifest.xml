<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.wgbn.pw_20160219_criando_atividades">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AtividadePrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SegundaAtividade"
            android:label="@string/title_activity_segunda_atividade"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TerceiraAtividade" />
        <activity android:name=".QuartaAtividade"></activity>
    </application>

</manifest>
